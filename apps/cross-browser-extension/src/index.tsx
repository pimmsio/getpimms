// Main entry point for CRA compatibility
// This file is required by Create React App but not used in the extension
import { logger } from './utils/logger';
logger.debug('PIMMS - Main entry point loaded');